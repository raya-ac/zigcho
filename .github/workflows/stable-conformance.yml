name: stable conformance contracts

on:
  pull_request:
    paths:
      - src/**
      - .github/workflows/stable-conformance.yml
  push:
    branches:
      - main
    paths:
      - src/**
      - .github/workflows/stable-conformance.yml
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: stable-conformance-${{ github.ref }}
  cancel-in-progress: true

jobs:
  contracts:
    uses: zigcho/stable-conformance/.github/workflows/check.yml@37ff441bc6fbe380c28fbc07a80790bcf3f78bc6
    with:
      harness_ref: 37ff441bc6fbe380c28fbc07a80790bcf3f78bc6
      zigcho_ref: ${{ github.sha }}
