name: stable conformance contracts

on:
  pull_request:
    paths:
      - src/**
      - .github/workflows/stable-conformance.yml
  push:
    branches:
      - main
    paths:
      - src/**
      - .github/workflows/stable-conformance.yml
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: stable-conformance-${{ github.ref }}
  cancel-in-progress: true

jobs:
  contracts:
    uses: zigcho/stable-conformance/.github/workflows/check.yml@c0d0686c69db754dedaa00ef3b7ed084477893b1
    with:
      zigcho_ref: ${{ github.sha }}
